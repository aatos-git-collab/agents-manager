interface:
  display_name: "aatosteam"
  short_description: "Create and coordinate multi-agent teams from Codex."
  default_prompt: "Use $aatosteam to create a team, split this task into tracked work items, spawn workers, coordinate them, and deliver the result."

policy:
  allow_implicit_invocation: true
